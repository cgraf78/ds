name: CI

on:
  push:
    branches: [master, main]
  pull_request:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  shell:
    uses: cgraf78/actions/.github/workflows/shell-ci.yml@e3246b432e347b5c331940d41626cc7bda60b803
    with:
      matrix-set: full
      profiles: base,openssh-netcat-lsof,procps,shellcheck
      termux-profiles: base,shellcheck
      test-command: bash tests/ds-ci
      termux-command: |-
        shellcheck -x bin/ds
        shellcheck -x lib/plugins/*.sh
        shellcheck -e SC2317 -x -P SCRIPTDIR tests/ds-test tests/test-helpers.sh
        bash bin/ds --help
