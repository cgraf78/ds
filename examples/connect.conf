# Example connect config for ds
# Maps hostnames (glob patterns supported) to connection methods.
# Place at ${XDG_CONFIG_HOME:-$HOME/.config}/ds/connect.conf
#
# hostname    connect-method
# myserver    autossh
# dev*        ssh
# localbox    -
