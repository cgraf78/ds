# Example Configs

These files are sample `ds` configuration snippets. They should demonstrate
real plugin wiring without requiring local machine-specific paths or private
hosts.

When adding an example, keep comments focused on the setting being demonstrated
and make sure the referenced plugin exists under `lib/plugins/`.
